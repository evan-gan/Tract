// Copy to Local.xcconfig (gitignored) and fill in your own values.
// BUNDLE_PREFIX    — reverse-DNS prefix; the app bundle id becomes $(BUNDLE_PREFIX).app
// DEVELOPMENT_TEAM — 10-character Apple Developer Team ID
BUNDLE_PREFIX = com.spline
DEVELOPMENT_TEAM = YOUR-TEAM-ID
